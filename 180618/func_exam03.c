#include<stdio.h>
void put_nchar(int ch, int no) {
	//
}
int main() {
	char ch;  int num;
	printf("문자 입력: "); scanf("%c", &ch);
	printf("정수 입력: "); scanf("%d", &num);
	puts("");
	put_nchar(ch, num);
	puts("");
}
